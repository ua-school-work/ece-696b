package edu.arizona.josesosa.behavioral.broadcaster.common;

/**
 * Enum representing the types of messages that can be sent between components.
 */
public enum MessageType {
    /**
     * Text message type for simple text messages
     */
    TEXT,
    
    /**
     * Command message type for executable commands
     */
    COMMAND
}